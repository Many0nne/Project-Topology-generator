{
  "name": "__PROJECT_NAME__-backend-fastify",
  "version": "1.0.0",
  "description": "Minimal Fastify backend for __PROJECT_NAME__",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev --name init"
  },
  "type": "module",
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/cookie": "^10.0.0",
    "@fastify/helmet": "^12.0.0",
    "dotenv": "^16.3.1",
    "argon2": "^0.30.0",
    "jsonwebtoken": "^9.0.0",
    "@prisma/client": "^5.8.0"
  },
  "devDependencies": {
    "prisma": "^5.8.0"
  }
}
